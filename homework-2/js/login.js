/* TODO: See Part 3 of README.md */
const correctUsername = undefined;
const correctPassword = undefined;

const login = (username, password, errorDiv) => {
    console.log(`Username: ${username}`);
    console.log(`Password: ${password}`);
    console.log(`The error message in errorDiv reads "${errorDiv.children[0].textContent}"`);
    /* TODO: See Part 3 of README.md */
}

const setup = () => {
    /* TODO: See Part 2 of README.md */
}

export default setup;